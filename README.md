# codebase-onboarding-check

Vendor-neutral skill package. Only `SKILL.md` and `references/` are included so you can wire this into any coding agent (Cursor, Claude, OpenAI, etc.). To use with a specific provider, create your own `agents/<provider>.yaml` pointing at the skill path and preferred prompt.
